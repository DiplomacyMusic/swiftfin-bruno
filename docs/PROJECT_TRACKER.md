# Bruno — Project Tracker

> Canonical, living status board for the Bruno tvOS fork. The **bruno-expert** agent owns this file
> and keeps it in sync. When you finish a unit of work, update the matching row here in the same change.
>
> Inputs that feed this tracker (read them, don't duplicate them):
> - Product contract: `prototype/design_handoff_bruno/PRODUCT_SPEC.md` + `README.md` (the mockup)
> - Verified architecture / signatures: `BRUNO_NOTES.md`
> - Build & run recipes: `docs/STATUS.md`, `docs/SIM_VIEWING_HANDOFF.md`, `docs/DEPLOYMENT_HANDOFF.md`
> - Next-push design intent: `docs/UI_POLISH_ROADMAP.md`
> - Top Shelf wiring: `docs/TOP_SHELF_SETUP.md`
>
> Status legend: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked

_Last synced: 2026-06-27 (Movies vertical-scroll hitch FIXED/landed — three commits on `main`: (1) `60d858df` instruments the Movies/genre surface for the debug HUD — added `.brunoDebugRedraw("genre-shelf:…")` + `.brunoDebugLayout(…)` in `BrunoCategoryShelves.shelf(for:)`, mirroring Home's `BrunoShelfView:127-128`, so redraws/s + shelf Δy now report on the Movies tab; (2) `7985aaf0` lightens the genre cell — in `BrunoFocusArtCycle` the `@StateObject BrunoArtCycleViewModel` (+ its `BrunoPosterPrefetcher`) and all cycle `@State`/logic moved into a new private `ArtCycleOverlay` inserted ONLY while `isFocused`, so unfocused cells build a minimal graph (background + foreground, no visual change) — this was the PRIMARY fix; (3) `466aeb3f` repoints the `CollectionHStack` SPM dep from `LePips/CollectionHStack@main` to the fork `DiplomacyMusic/CollectionHStack@bruno-hosting-reuse` (`ce86c82` + one patch: `HostingCollectionViewCell` keeps its `UIHostingController` alive across reuse and swaps `rootView` instead of minting a fresh controller per `cellForItemAt` + tearing it down in `prepareForReuse` — that per-dequeue mint was the structural floor under the hitch, shared by Home AND Movies). Diagnosis evidence: on-device held-up-scroll, warm caches — Movies ran ~20fps with ~150ms·8f stalls per focus row-step; Home (identical architecture, lighter PosterButton cell) ran ~45fps / ~54ms; the delta isolated the heavier Movies cell as the dominant cost. New cell-reuse correctness rule captured as **INV-10** in `docs/BRUNO_PERF_INVARIANTS.md`. OUTSTANDING verification: an on-device re-record to confirm the gain (the device test was skipped before landing, by request). Backlog item `bebdfe30` (UIKit-drawn shelf cells) is now NOT needed for now / superseded by the hosting-controller reuse, unless on-device data later shows residual. Prior: Genre PILLS → 11 owner-curated buckets (G9) + sub-genre cap 100→120 (G5): the pill row is now Action & Adventure · Sci-Fi & Fantasy · Comedy · Drama · Romance · Crime · Thriller · Horror · History · Family · International, driven by an EXPLICIT exact-match `members` map (owner hand-tagged all 80 sub-genres in a Google Sheet) replacing the old 5 substring-keyword cores — every sub-genre now lands under ≥1 pill, duplication intentional, no accidental matches. Prior: Genre-surface gap-fixes pass (owner-directed, post-audit): Genres tile dropped from Home's mid-feed "Browse the Collection" shelf so Genres lives ONLY on the Movies tab (G1); core pills filtered to those with ≥1 sub-genre so a pill can never land on a blank surface (G3); empty-state now offers an "All Movies" escape at the tab root so a childless Genres group is never a dead end (G4); comment-accuracy fixes — "+6h" reshuffle documented as cold-launch-only on the always-mounted tab, four "~150 ms" debounce comments corrected to ~500 ms (constant was always 500) (G2/G7). Build-verified. Prior: Movies tab IS now the genre browse surface + genre rows fill — PR #19, merge `05ae924a`. The Movies tab repoints from the A–Z grid to a new `BrunoMoviesView` tab-root that resolves the **Genres** group and renders `BrunoGenresView(isTabRoot:true)`; the A–Z grid survives behind an "All Movies" pill (`.brunoMoviesGrid`, lazy). Sparse genre rows (the 1–4-card bug) are FIXED — the cross-category dedup in `performLoad` was sending each film to only the first sub-genre that claimed it; removed, so every genre row shows its full membership (modern-cutoff dropped on those rows too). Added a per-launch + per-6h ROW-ORDER reshuffle (`rowOrderSeed`, read once per load) so the genre tab feels fresh; the Featured Film hero rotates with it; within-shelf item order stays day-stable. Genres card removed from the Collections hub + Home footer; genre "Show all" cards now name the genre. INV-3 carve-out documented (genre/Movies row order is deliberately non-deterministic per launch/6h on this sub-surface; the Home spine stays deterministic). Prior: Enrichment → Bruno FILING MAP added below — how `MovieCollection/enrich/` sub-genres / Oscar-Ebert / Foreign+language / Vibes / discovery genres / Movie Stars / Best-of-Decade / bruno-sig file into the app. Governing fact: the app shelf engine has NO tag source — `BrunoQuery` speaks only genre-name/parentID/year/rating/person/studio — so every tag-membership must ship as a Jellyfin BoxSet under a group tile, which the app then renders with ZERO changes. ~90% build-script work; app-side is only Movie Stars (Directors clone) + Best-of-Decade ordering + any newly-promoted top-level group. No perf-invariant conflict for the BoxSet path. Prior: HUD-persist + pill-scroll pass: debug HUD hosted in a passthrough UIWindow so it floats above drill-down pushes; Decade/Kids pill-select scroll-jumps to the pills-at-top framing; Kids sparse-filter Down-focus fix; up-nav hero no longer swallows Up so focus reaches the tab bar. Prior: Perf + browse-completeness pass: lean poster fields ([.parentID]-only cells) + warm-snapshot reuse on un-forced refresh; new BrunoItemPaging (page-to-completion) replaces hard-limited A-Z/Kids GetItems; release-date subtitle on Decades shelves; per-year shelves on a selected decade + Other/Undated catch-all; focus-to-select Genre/Decade pills with "All" chip + hero decoupled; browse drill-in cache actor; poster DataCache 1GB→10GB. Followed a completeness AUDIT + adversarial plan review; several limit-cuts deferred for owner sign-off / on-device measurement. Collections pass 4: Studios→landscape, genre shelves shuffled (seeded, not alphabetical), studio logos=transparent-Logo-only-else-name-card + studio blurbs/Overviews, "Before Trilogy Collection" rename; Hollywood-sign Studios backdrop attempted 4× and REVERTED — proper redesign specced in docs/STUDIO_GRID_HANDOFF.md. Pass 3: raised box-set fetch cap so LOTR/Star Wars show; Boxed Sets card "Title Collection" + film count + dimmed year range w/ overflow; Decades → pill selector; live-renamed Before Trilogy Collection; builder script adds franchise logo-thumbs + Coen-wrapper cleanup. Pass 2: decades newest-first, Genres drill-in drops the sub-genre card row + lazy in-place core switch; builder script gains 4 romance sub-genres + studio-logo posters — server-side, needs an owner run). Pass 1 same day: code-drawn category tiles, fixed top-shelf order, hero horror filter, director-collection exclusion from Boxed Sets, landscape Boxed Sets grid w/ collection lockup + header-overlap fix. Update this date on every edit._

---

## Now / next (the active push: tvOS IA + browse polish)

| St | Item | Where | Notes / blocker |
|----|------|-------|-----------------|
| [~] | Tab IA reorder → **Home · Movies · TV Shows · Collections · Kids · Search · Settings** | `Shared/Coordinators/Tabs/MainTabView.swift`, `TabItem.swift` | Decided 2026-06-23. **CONTENT done (`d685b68c`):** Movies tab now = the genre browse surface (`BrunoMoviesView` → `BrunoGenresView`), Collections tab exists. **Tab POSITION/order reorder is still separate/not done** — `MainTabView.swift` tab ordering unchanged. |
| [!] | `TabItem.kids` → existing Jellyfin kids library/folder | `TabItem.swift`, `BrunoKidsView.swift` | **BLOCKED:** need exact Kids library name(s)/id(s) on the server. |
| [~] | System Top Shelf extension (dynamic previews) | `BrunoTopShelf/`, `BrunoTopShelfCredentials.swift` | Groundwork shipped; owner must create the target + App Group + signing. See `docs/TOP_SHELF_SETUP.md`. |
| [ ] | Night-mode / settings surface | (TBD) | Called out as outstanding in the latest handoff. Scope undefined. |
| [ ] | On-device shelf check (focus feel, scroll-jump landing) | Home / Collections / Genres | Category-row scroll-jump + hero focus feel want a real Apple TV pass. |
| [x] | **Movies/browse vertical-scroll hitch → FIXED via cell-reuse fork + focus-gated genre cell** | `CollectionHStack` SPM dep (now forked), `BrunoFocusArtCycle`, `BrunoCategoryShelves.shelf(for:)` | **LANDED on `main` 2026-06-27** in three commits — `60d858df` (HUD instrumentation on the Movies/genre surface) + `7985aaf0` (PRIMARY fix: genre cell's heavy `@StateObject BrunoArtCycleViewModel`/prefetcher/cycle `@State` moved into a focus-gated `ArtCycleOverlay`, mounted only while `isFocused`; unfocused cells build a minimal graph, no visual change) + `466aeb3f` (repoint `CollectionHStack` from `LePips/CollectionHStack@main` to the fork `DiplomacyMusic/CollectionHStack@bruno-hosting-reuse` = `ce86c82` + one patch that keeps `HostingCollectionViewCell`'s `UIHostingController` alive across reuse and swaps `rootView` instead of minting/tearing-down per `cellForItemAt`/`prepareForReuse` — the structural floor under the hitch, shared by Home AND Movies). **Diagnosis evidence:** on-device held-up-scroll, warm caches — Movies ~20fps / ~150ms·8f stalls per focus row-step vs Home ~45fps / ~54ms (identical architecture, lighter PosterButton cell) → isolated the heavier Movies cell. **The fork patch contradicts the prior "per-cell mint is identical in stock so a dependency fork is NOT the right fix" diagnosis** (kept below for history) — the fork lifts the shared floor for both surfaces, and the focus-gate removes the Movies-specific overhead. New cell-reuse correctness rule = **INV-10** (`docs/BRUNO_PERF_INVARIANTS.md`). **OUTSTANDING:** an on-device re-record to confirm the gain (device test skipped before landing, by request). Prior diagnosis (now superseded — kept for context): root cause found 2026-06-24, each reveal ~200-450ms; per-cell `UIHostingController` mint "identical in stock Swiftfin" so a fork was thought wrong; the proposed fix was "comply with Swiftfin" by routing flat-category browse to the grid + capping carousels. Levers that DON'T help: `dataPrefix` (already matched), `drawingGroup` (breaks focus). |
| [ ] | **Collection "card-deck" card** | `BrunoBoxSetGridView` / `BrunoBoxSetCardLabel` | Deferred from the 2026-06-25 Collections pass. Replace the single landscape poster with a "deck" look: the main cover plus 2 other titles in the collection peeking from behind on the right, total width = one landscape card. For now the grid uses a plain landscape card (fixes the scrunched text). |
| [ ] | **Server curation: orphan director collections** | Jellyfin server (owner) | The Boxed Sets grid now excludes director collections by id (nested under Directors) and by name-match against the Directors group's children. A standalone director collection that is neither nested nor name-identical will still leak in. Durable fix is server-side: add such collections under the **Directors** group. The DEBUG log `[Bruno] Boxed Sets (…)` in `BrunoCollectionsViewModel.load()` lists the current set so strays are easy to spot. |
| [ ] | **Genre BoxSet membership cleanup + 100% coverage audit** | External enrich pipeline + live Jellyfin server (owner; additive-only API → needs targeted `DELETE /Collections/{id}/Items`) | Follow-up from PR #19 (genre rows now show FULL membership, so bad entries are visible). Removals: Alien Movies − "Crystal Skull"; Cubicle − "Fargo"; Disaster + "Independence Day" + "Armageddon"; Buddy Cop − duplicate "Men in Black (1997)". Then AUDIT every movie into ≥1 genre BoxSet (100% coverage). |
| [x] | **Genre pills → 11 owner-curated buckets w/ explicit member map (G9)** | `BrunoGenresView.swift` (`BrunoCoreGenre`) | Replaced the 5 substring-keyword cores with **11 buckets** (Action & Adventure · Sci-Fi & Fantasy · Comedy · Drama · Romance · Crime · Thriller · Horror · History · Family · International) and an **explicit exact-match `members: Set<String>`** map, hand-assigned by the owner in a Google-Sheet tagging pass. All 80 sub-genres now land under ≥1 pill (nothing invisible); duplication is intentional; no accidental substring matches. Maintenance: exact-name match — rename a BoxSet on the server ⇒ add the new name to its bucket(s). |
| [ ] | **Browse → Home realignment redesign** | Collections / Genres / Decades browse surfaces | Browse drifts from Home in 3 ways: (1) no persistent hero banner — **owner confirmed (2026-06-23) they want the cinematic featured-content banner (à la Home's `BrunoHeroView`, NOT a streamer logo) to persist across browse views** (the Genres/Movies surface already carries a per-surface Featured Film hero that now rotates per launch/6h — `01e05da7` — but cross-navigation persistence is still open), (2) eyebrows are repeated group-names rather than lenses, (3) flat vs elevated posters. Make browse consistent with Home on color/type/spacing. Cheapen `.posterShadow()` *with* on-device measurement here (keep the shadow — it's brand, README.md:189-193); revisit `.hoverEffect(.highlight)` removal under measurement. Brief authored 2026-06-23. |

### Movies/genre surface — audit follow-ups (`docs/BRUNO_MOVIES_GENRE_SURFACE.md` §8)

> Verified gap list from the post-ship code audit of PR #19. One tight row each — full detail (repro,
> repair sketch, owner-decision framing) lives under §8 G1–G10 in that doc. Surface-only; all Bruno-owned.

| St | Item | Where | §8 ref |
|----|------|-------|--------|
| [x] | **Genres tile gone from Home's mid-feed "Browse the Collection" shelf** (owner-directed) — the third builder's `items` now `.filter { name != "genres" }`, so Genres lives only on the Movies tab. | `Shared/Objects/Bruno/BrunoHomePlan.swift` | G1 |
| [x] | **Core-genre zero-match blank surface — blocked by prevention** — the pill row now renders only `shownCoreGenres` (cores matching ≥1 loaded sub-genre), so a dead core is never selectable. | `BrunoGenresView.swift` (`shownCoreGenres`) | G3 |
| [x] | **Childless-group dead screen — escape added** — the empty-state now shows an "All Movies" button at the tab root (`onShowAll != nil`), so the Movies tab is never a dead end. | `BrunoGenresView.swift` (`emptyState`) | G4 |
| [x] | **Sub-genre fetch cap raised `100 → 120` (G5)** — headroom over the ~80 sub-genres (a comment says page to completion if it ever approaches the cap). | `BrunoBoxSetShelvesView.swift` | G5 |
| [x] | **Comment-accuracy fixes landed (no code-constant change):** the "+6h" comment now states cold-launch-only reality; the four "~150 ms" debounce comments now say ~500 ms (the constant was always 500). | `BrunoGenresView.swift`, `BrunoBoxSetShelvesView.swift` | G2 · G7 |

> Also tracked above as standalone rows: G9 = the `BrunoCoreGenre.keywords` expansion row. Not surfaced
> (lower-priority / staged): G6 (no poster prefetcher on this surface), G8 (long `Show all · <genre>`
> label truncation), G10 (dead decades-only machinery on this VM) — see §8.

## Done (recent, newest first)

- [x] **Movies vertical-scroll hitch FIXED — cell-reuse fork + focus-gated genre cell** (3 commits on `main`, newest-first `466aeb3f` · `7985aaf0` · `60d858df`) — all Bruno-owned/additive, tvOS-only; no perf invariant broken (and INV-10 added to codify the new rule). Three landed changes:
  - **HUD instrumentation on the Movies/genre surface** (`60d858df`) — added `.brunoDebugRedraw("genre-shelf:\(category.name)")` + `.brunoDebugLayout(…)` in `BrunoCategoryShelves.shelf(for:)`, mirroring Home's `BrunoShelfView:127-128`, so redraws/s + shelf Δy now report on the Movies tab (made the hitch measurable on-surface).
  - **Lighten the genre cell — PRIMARY fix** (`7985aaf0`) — in `BrunoFocusArtCycle`, the `@StateObject BrunoArtCycleViewModel` (+ its `BrunoPosterPrefetcher`) and all cycle `@State`/logic moved into a new private `ArtCycleOverlay` that is inserted ONLY while `isFocused`. Unfocused cells now build a minimal graph (background + foreground). No visual change. This removed the Movies-specific per-cell overhead that made it heavier than Home.
  - **Repoint `CollectionHStack` to a hosting-controller-reuse fork** (`466aeb3f`) — the SPM dep moves from `LePips/CollectionHStack@main` to `DiplomacyMusic/CollectionHStack@bruno-hosting-reuse` (a fork of `ce86c82` + one patch). The patch: `HostingCollectionViewCell` keeps its `UIHostingController` alive across reuse and swaps `rootView` on recycle, instead of minting a fresh controller per `cellForItemAt` + tearing it down in `prepareForReuse`. That per-dequeue mint was the structural floor under the hitch, and it is SHARED by Home and Movies — so the fork lifts both surfaces' floor at once.
  - **Diagnosis evidence:** on-device held-up-scroll, warm caches — Movies ran ~20fps with ~150ms·8f stalls per focus row-step; Home (identical architecture, lighter PosterButton cell) ran ~45fps / ~54ms. The delta isolated the heavier Movies cell as the dominant cost; the fork patch then lifts the shared floor for both. **This supersedes the earlier "a dependency fork is NOT the right fix" reading** (which held the per-cell mint was identical-to-stock and unfixable without a Swiftfin-style grid rewrite). **OUTSTANDING:** owner has NOT yet re-recorded on device to confirm the gain (the device test was skipped before landing, by request). New cell-reuse correctness rule captured as **INV-10** in `docs/BRUNO_PERF_INVARIANTS.md`.
- [x] **Movies tab becomes the genre browse surface + genre shelf rows fill** (PR #19, merge `05ae924a`; commits `01e05da7` + `d685b68c`) — all Bruno-owned/additive, tvOS-only; build-verified (headless clean `xcodebuild` BUILD SUCCEEDED, 0 errors, Swiftfin tvOS) + swift-xcode-expert review (actor-isolation / `@MainActor` route / `.if`-at-constant-condition / re-entrancy all clear); merged to `main` (local + origin both at `05ae924a`). Two commits:
  - **Genre rows fill + per-launch/6h row reshuffle** (`01e05da7`) — root cause of sparse genre browse rows (1–4 cards) was the cross-category dedup in `BrunoBoxSetShelvesViewModel.performLoad` (each film went to only the FIRST sub-genre row that claimed it); REMOVED so every genre row shows its full sub-genre membership, and DROPPED the 1985 modern cutoff on those rows (healthy Action/Comedy rows unchanged). Added a per-launch + per-6h **row-order** reshuffle: `launchNonce` (static, once per process) `&+` a 6h wall-clock bucket → `rowOrderSeed`, read ONCE per load in `performLoad`, shuffles the genre category order via `BrunoRNG.shuffled`; the Featured Film hero rotates with it (`BrunoGenresView` computes the hero over the shuffled `viewModel.categories`). Within-shelf item order stays day-stable (separate `shuffleSeed`); Decades (newest-first) and Curated (server order) untouched (non-`recencyBiased` branch). Deleted the now-orphaned `dedupeAcrossCategories` and `BrunoRecencyBias.modernOnly`/`isModern`; KEPT `BrunoRecencyBias.modernCutoff` (still used by `BrunoHomePlan.genreQuery`'s server-side year filter + the Classic Romance split). **INV-3 carve-out documented** in `docs/BRUNO_PERF_INVARIANTS.md`: genre/Movies row order is intentionally non-deterministic per launch/6h on this sub-surface (seed read once per load → stable within a session, INV-7 safe); the Home spine stays deterministic.
  - **Movies tab → genre browse surface** (`d685b68c`) — the Movies tab is repointed (`Shared/Coordinators/Tabs/TabItem.swift`, `static var movies`) from `BrunoMediaView(itemType:.movie)` to a NEW thin tab-root `BrunoMoviesView` (`Swiftfin tvOS/Views/BrunoHomeView/BrunoMoviesView.swift`) that resolves the **Genres** group from `BrunoLibrarySnapshot.loadShared` (`favoriteGroupBoxSets.first{ name=="genres" }`) and renders `BrunoGenresView(parent:group, core:nil, isTabRoot:true, onShowAll:…)`; falls back to the A–Z `BrunoMediaView` grid if the group is absent / snapshot empty. The A–Z grid is preserved behind a trailing **"All Movies"** pill in the core-genre pill row → pushes the lazy `NavigationRoute.brunoMoviesGrid` (fetches only on push; `selectsOnFocus:false`, pure navigation — never commits a genre filter). `BrunoGenresView` gained stored `isTabRoot` + `onShowAll` (init widened, defaults `false`/`nil` keep the existing `.brunoGenres` cover route working); as the tab root it SUPPRESSES its self-applied `.brunoHeroMenuBar()` via `.if(!isTabRoot)` (MainTabView supplies the bar to tab roots — re-pinning would double-bar, the e44e1e71 regression; same precedent as the Collections tab root). The **Genres** card is removed from BOTH the Collections hub and the Home feed footer via one guard (`guard name.lowercased() != "genres"`) in the shared `BrunoCollectionCategory.fromSnapshot` (`BrunoCategoryShelves.swift`). Genre shelves now name their "Show all" card with the genre ("Show all · Time Travel") via an opt-in `namesShowAllCards`/`showAllTitle` threaded `BrunoCategoryShelves` → `BrunoShelfRow`; other surfaces keep the generic "Show all". NO movies-only filter (the ~80 sub-genre BoxSets are already movie-only server-side; a `type==.movie` filter on the group's `.boxSet` children would blank the page); reuses the height-pinned INV-compliant `BrunoCategoryShelves` (no new shelf-stacking). **TV Shows unchanged** (still `BrunoMediaView`). Movies-tab/focus runtime QC is on-device (the plan scopes focus checks to a real Apple TV; headless sim-nav can't drive tab focus); fresh sim build launches + Home renders.
- [ ] **Up-nav: Up from a drilled-in page can't reach the app tab bar — OPEN (earlier attempt REVERTED)** — the hero (`BrunoHeroView`) `.onMoveCommand` consumes Up (no-op `default`), so focus can't rise to the tab bar; only the hero-less Settings page works. A first attempt REMOVED the handler entirely — but that killed the hero's left/right spotlight swipe AND was never on-device-verified (on a pushed drill-down the tab bar may not even be in the focus tree, so removing the swallow likely wouldn't help there anyway). REVERTED to restore left/right. The correct fix must keep left/right AND let Up escape upward — the UIKit-responder approach in the backlog (handle left/right, pass Up/Down to the focus engine; on a push, Up at the top may need to pop). Must be on-device-verified before claiming done. → full analysis + resolution in `docs/BRUNO_HERO_UPNAV.md`
- [x] **Debug HUD persists on drill-downs + pill-select scroll framing (Decades/Kids) + Kids sparse-content focus fix** — all Bruno-owned/additive, tvOS-only, no perf invariant (INV-1..9) touched; build-verified (Swiftfin tvOS, Debug). (1) **HUD persists on pushed surfaces** — the DEBUG overlay HUD was a root `.overlay` that the full-screen NavigationStack drill-ins (`BrunoCategoryShelves` / `BrunoBoxSetShelvesView` / `BrunoStudiosGridView` / Kids) covered; now hosted in a passthrough `UIWindow` one level above the app window (tvOS only — `BrunoDebugOverlayWindow` in `BrunoDebugOverlayView.swift`, driven from the modifier's `sync()`), so the frame-drag log shows on the browse/decade surfaces where the drag happens. iOS path unchanged. (2) **Pill-select scroll framing** — soft-selecting a Decade/Kids pill jumps to the "pills near top, first shelf in full view" framing (a `ScrollViewReader` + `.id` selector anchor in `BrunoCategoryShelves`, driven by the COMMITTED/debounced `selectedDecade`; Kids drives its own reader off the committed `filter`). Honors reduce-motion (INV-9). (3) **Kids sparse-content focus fix** — a sparse filter (e.g. TV Shows = one row) shoved the pills up and BLOCKED Down-focus to that lone row; the grid region now reserves a measured-viewport `minHeight` (`BrunoKidsView`) so the row keeps empty space beneath it and stays Down-focusable. **Staged (await owner assets):** #1 browse vertical-scroll drag (now diagnosable on-surface via the persistent HUD; needs on-device hitch logs). (#2 up-nav → app tab bar is now fixed — see the entry above.)
- [x] **Perf foundation + browse-completeness + Decades/pills pass** (6 commits `00c6d790`..`c3341149`, on top of `a197dfc5`) — all Bruno-owned/additive; no perf invariant (INV-1..9) broken (each item INV-checked). Followed a multi-agent drill-down **completeness AUDIT** (studio/director/decade/genre/A-Z/show-all/collection — the drill-down destinations already paginate fully; the only real exposures were *latent ceilings*) + an adversarial expert review of all four workstream plans.
  - **Lean poster fields + warm-snapshot reuse** (`00c6d790`) — `BrunoQueryLibrary` poster-cell fetches now request only `[.parentID]` (dropped heavy `.mediaSources`/`.overview`/`.taglines`; detail/Play re-fetch full fields). `forceReload` threading in `BrunoHomeViewModel` so `.refresh`/`.backgroundRefresh` reuse the warm 300s snapshot cache; only `.shuffle` forces (INV-5 safe).
  - **Page-to-completion** (`550f1621`) — new `Shared/Objects/Bruno/BrunoItemPaging.swift` (append-only `startIndex` loop, pageSize 200, short-page stop, 50-iter cap, single-request early-exit). `BrunoMediaView` (A-Z Movies/TV) + `BrunoKidsView` converted from a single hard-limited `GetItems` (1000 / 400 — a *latent* ceiling) to paging-to-completion; hero pool derived from the full set; kept in the shared hero+grid scroll plane (NOT routed through `PagingLibraryViewModel`).
  - **Release-date subtitle on Decades** (`641969eb`) — additive `showsDate` flag (`BrunoCategoryShelves` → `BrunoShelfRow`); only the **Decades** surface opts in. New Bruno-local `BrunoTitleDateContentView` renders the full premiere date in the otherwise-blank-for-movies subtitle line (font/geometry-exact clone of tvOS `TitleSubtitleContentView`, INV-1 height unchanged; single static `DateFormatter`).
  - **Per-year decade shelves** (`9a3fff1c`) — selecting a decade pill now shows **one shelf per year** (newest-first) + an **"Other/Undated"** catch-all so nothing is dropped; "All" keeps the overview, "1950s & Earlier" stays one shelf. Fetches the decade's COMPLETE set via `BrunoItemPaging` (memoized per decade id), buckets by `premiereDate`-year (UTC) / `productionYear`; each year's "Show all" → a live year-filtered `ItemLibrary` via new `gridParent`/`gridYear` on `BrunoCollectionCategory`. Deterministic, stable synthetic ids.
  - **Focus-to-select pills + "All"** (`291134df`) — Genre & Decade pills now filter **on focus** (matching Kids) with a first-class "All" chip. Hero **decoupled** from the filter (computed once from the full set, held in `@State`); focus-vs-commit split with a ~150ms cancellable debounce (one shelf rebuild / one per-year fetch per *settled* pill, not per scrub); instant highlight off the focused value; INV-7 first-focus guard (`filterRowAppeared` + hero-first layout); toggle-to-clear removed; `.bucket` style kept.
  - **Browse drill-in cache** (`c3341149`) — new `BrunoBoxSetShelvesCache` actor keyed `(userID, parentID)`, 300s TTL, re-entrancy `loadTask` guard, so re-entering Genres/Decades/Curated skips the 20+ request fan-out (INV-5 safe).
  - **Poster disk-cache 1GB → 10GB** (`63033997`) — image poster `DataCache` `sizeLimit`; plus a SwiftFormat reflow of that line (`76c5bb7b`).
- [x] **Studios landscape + genre shuffle + studio logos/blurbs; Hollywood backdrop reverted (pass 4)** — **Bruno (built):** (1) **Studios "Show all" → landscape** grid (Directors stay portrait); both route through `BrunoBoxSetGridView` (header-overlap fix). (2) **Genre shelves shuffled** — `BrunoBoxSetShelvesViewModel` seeds a day-stable `BrunoRNG.shuffled` per shelf so rows aren't alphabetical. (3) **Hollywood-sign cinematic backdrop for the Studios grid: attempted 4×, REVERTED** — full-bleed photo behind a grid didn't land aesthetically; Studios is back to a clean landscape logo grid. The redesign the owner actually wants (cinematic focused-studio look + HQ logos + focus carousel) is specced in **`docs/STUDIO_GRID_HANDOFF.md`** (not implemented). **Builder script (edited; owner-run pending):** studio cards use **transparent Logo only**, else a clean branded **name card** (no logo-intro screenshots); **studio Overviews** set from `studio_blurbs.json` (92 studios, founder/notable titles/directors/trivia); studio 16:9 **Thumb** for the landscape grid. **Server (live):** renamed "Before… Collection" → "Before Trilogy Collection".
- [x] **Boxed Sets card polish + missing-franchise fix + Decades pills + franchise logos (pass 3)** — **Bruno (built):** (1) **LOTR / Star Wars (and other late-alphabet franchises) were missing** purely because `fetchAllBoxSets` capped at 200 of 304 box sets — raised to 1000. (2) **Boxed Sets card label reflow** (`BrunoBoxSetCardLabel`): line 1 = "{Title} Collection", line 2 = "N films" (left) + release-year range (right, fetched per-collection min–max child year via `BrunoBoxSetYearRangesViewModel`, gated to landscape). `ViewThatFits` overflow: long titles drop "Collection" to line 2 ("Collection · N films"). Year range rendered a step quieter (`.opacity(0.6)`) than the count. Grid rebuilds once (`.id(done)`) when ranges finish, since CollectionVGrid (UIKit) won't re-render cells on async update. (3) **Decades → Genres-style pill selector** (`BrunoBoxSetShelvesView`, decades-only): replaced the big decade cards with `BrunoSelectorCard` pills + in-place filter. **Server (live, done now):** renamed "Before... Collection" → **"Before Trilogy Collection"**. **Builder script (edited + py-compiled; owner run pending):** franchise collections get a **member-film LOGO on a 16:9 card as their Thumb** (`make_logo_thumb`/`set_thumb`) so Boxed Sets cards show logos not screenshots; **DIRMAP cleanup deletes redundant standalone "Joel Coen"/"Ethan Coen" box sets** on next run (live delete was blocked as destructive — runs via the script). **Already correct server-side:** Planet of the Apes is split into "(Original)" n=3 and "(Reboot)" n=2.
- [x] **Collections/Genres browse polish + builder-script romance & studio logos (pass 2)** — two codebases. **Bruno tvOS (built + sim-verified):** (1) **Decades newest-first** — `BrunoBoxSetShelvesView.load()` sorts decade sub-groups by leading year descending (`leadingYear` helper), dropping "1950s & Earlier" to the bottom; decades-only, other groups keep server order. (2) **Genres drill-in: dropped the sub-genre card row** (`showCategoryRow: false`) — relies on each shelf's "Show all" → grid. (3) **Lazy genre switch** — `BrunoGenresView` core selector no longer pushes a new view + refetches; it's an in-place `@State selectedCore` that re-filters the already-loaded set (instant, no spinner), with chip highlight + toggle-to-clear; added `romcom`/`rom-com` keywords to the Romance bucket so the new "RomCom All-Timers" shelf matches. **Builder script `~/Documents/Claude/MovieCollection/Build-Jellyfin-Collections.command` (edited + py-compiled; OWNER must run it to apply to the live server):** (4) **4 romance sub-genres under the Genres tile** beside Romantic Comedy/Drama — Classic Romance (Romance ≤1979), RomCom All-Timers (Romance AND Comedy by Roberts/Ryan/Hanks/Bullock/Ephron/Meyers, OR explicit titles incl. Jerry Maguire/Dirty Dancing/Road House), Bromance (Top Gun/Jerry Maguire/Say Anything/Cocktail/Before-trilogy/Casablanca/Doctor Zhivago), Teen Romance (Pretty in Pink/10 Things/Can't Hardly Wait); cast extraction added (`People` already fetched). (5) **Studio collections now use the STUDIO LOGO** as their poster (composited centered on a branded card via `make_logo_poster`, alpha-respected), instead of a member-movie still; falls back to a member poster when a studio has no logo. **Pending: owner runs the .command** to materialize #4/#5; new titles (Doctor Zhivago, Can't Hardly Wait) populate once on disk.
- [x] **Collections tab expansion (heavy on Boxed Sets)** — 7-ask pass, all Bruno-owned/additive, no `.pbxproj`/stock edits. (1) **Hero child-safety:** new `brunoHeroEligible(_:)` rejects any Horror-tagged item (substring) from EVERY hero pool (Collections/Genres/Decades via `brunoFeaturedItem`; Movies/TV `BrunoMediaView`; Kids `BrunoKidsView`; Home `BrunoHomeViewModel` — filtered on the input pool, INV-3 determinism preserved; also re-filtered on disk-cache hydrate). `.genres` added to the two Collections snapshot child fetches (`BrunoLibrarySnapshot.fetchChildren`, `BrunoBoxSetShelvesView`) since `MinimumFields` omitted it (the other heroes already requested it). (2) **Code-drawn category tiles** — new `BrunoCategoryTile` replaces the per-group server poster in `categoryCardRow`; controlled-size Oswald label fixes the giant "NEW" and gives the synthetic "Boxed Sets" tile real art (was a grey placeholder). Stays on `CollectionHStack` + `.buttonStyle(.card)` for native focus. (3) **Fixed top-shelf order** — New Releases · Genres · Directors · Boxed Sets · Decades · Curated · Studios · Seasonal, via a stable (index-decorated) rank sort in `BrunoCollectionsViewModel.load()`; unknowns to end. (4) **Director collections excluded from Boxed Sets** — id-exclusion + name-match against the Directors group's children (`trimmedLowercased`); DEBUG log of the final set. (5/6/7) **Landscape Boxed Sets grid** — new `BrunoBoxSetGridView` (dedicated `CollectionVGrid`, NOT stock `PagingLibraryView` whose poster style is user-global) + `.brunoBoxSetGrid` route; landscape cards with the `BrunoBoxSetCardLabel` lockup (title sans " Collection" / "Collection" / dim-grey "N films" from `.childCount`); Directors/Studios sub-collection grids routed through it in portrait so they inherit the **header-no-longer-overlaps-first-row** fix (the view owns its top inset instead of ignoring the top safe area). New `String.brunoStrippingCollectionSuffix` / `.trimmedLowercased`. `BrunoStaticItemsLibrary`/`.brunoItemsGrid` now unused (left in place — flagged). No perf invariant (INV-1..9) touched (grid is vertical CollectionVGrid; category row keeps CollectionHStack).
- [x] **Debug overlay HUD for nav/UX hitch diagnosis** (DEBUG-only, tvOS + iOS). On-screen HUD with three independently-toggleable panels for diagnosing UX/navigation hitches and frame drag: **FRAME** (live FPS, frame time, worst frame, hitch count, frame-time sparkline; CADisplayLink-driven — ground truth on long/dropped frames), **NAV/LAYOUT** (redraws/sec per tracked view + recent layout movements (shelf Δy) and focus events — the "graphic math" nav triggers), **LOG** (timestamped event stream mixing layout + frame-drag events; any long frame within 0.5s of an interaction is tagged "(nav)" to separate nav-induced drag from background work). Toggles live at the **top of Settings** in a new "Debug Overlays" section (DEBUG builds only). New: `Shared/Objects/Bruno/BrunoDebugCore.swift` (frame monitor + log bus), `BrunoDebugOverlayView.swift` (HUD + layout/nav modifiers), `BrunoDebugInstrument.swift` (always-compiled public API — real in DEBUG, no-op in release, so call sites carry no `#if`). Edited: `SwiftfinDefaults.swift` (3 DEBUG keys brunoDebugFPS/Nav/Log), `SettingsView.swift` (top section), both `SwiftfinApp.swift` roots (`.brunoDebugOverlay()`), `BrunoShelfView.swift` + `BrunoShelfRow.swift` (instrumented with `.brunoDebugRedraw`/`.brunoDebugLayout`/`.brunoDebugNavFocus`). Builds pass on both schemes; runtime-verified on the tvOS sim (all three panels render, toggle independently, capture real shelf-layout Δy + frame drags). Zero release cost (DEBUG-gated; CADisplayLink only runs while a panel is on). Additive — no perf invariant (INV-1..9) touched.
- [x] **Landscape shelf hard-snap fix** — landscape shelves were unpinned (only portrait was), so an up-nav focus-move renegotiated their height and hard-snapped the scroll. Pinned landscape too (`BrunoShelfMetrics.landscapeShelfRowHeight = 348`); sim-verified fit. Also capped poster prefetch to a screenful. INV-1 now covers both orientations.
- [x] **Selector-pill ("bubbles") highlight fix** — the accent focus ring was an `if`-inserted view that popped mid-scale; now always-present + opacity-animated so it cross-fades with scale/fill (`BrunoSelectorCard`).
- [x] **Home snappiness pass** (on `main`, branch `claude/tvos-perf-snappiness`). Four workstreams, all Bruno-owned/additive, build-verified:
  - **Streaming Home paint** — hero published the moment it lands (no more waiting on all ~18 shelves); shelves stream in top-down in plan order via a flush-consecutive reveal (load parallel, reveal shelf *i* only once 0..*i* settle → append-only, never shifts shown rows). Same for the explore tail. Generation-guarded. Soft fade+drift reveal (reduce-motion aware); hero auto-advance held until spine settles. `BrunoHomeViewModel`, `BrunoHomeView`, `BrunoHeroView`, `BrunoShelfView`.
  - **Disk-persisted Home payload + stale-while-revalidate** — `BrunoLibrarySnapshot` is `Codable`; new `BrunoHomeCache` (actor, off-main I/O, `try?`-tolerant) persists snapshot + hero superset pool + `.query` items, seed+userID-keyed. Relaunch paints instantly, then revalidates and reconciles in place by `shelf.id` (identity/focus survive). Live rows (`.resume`/`.nextUp`/`.recentlyAdded`) never cached.
  - **Bruno-owned poster prefetch** — `BrunoPosterPrefetcher` warms each row on appear via `.Swiftfin.posters` at the cell's exact width (`BrunoShelfMetrics`), low-priority, cancel-on-disappear. No stock edits. (spec §6 L159)
  - **Fragility guardrails** — `BrunoShelfMetrics` centralizes the 460 height + poster widths; `docs/BRUNO_PERF_INVARIANTS.md` documents INV-1..9 (what/why/break/safe-recipe + safe-to-touch surface); `// INV-n` anchors at each site. **Browse streaming deferred** into the Browse→Home redesign (shared-file collision); prefetch lands on the shared `BrunoShelfRow` row.
- [x] Browse vertical-scroll perf: fewer realized poster cells per row (browse only; Home untouched) — `BrunoCategoryShelves.swift` `shelfCap` 36→14, `BrunoShelfRow.swift` `.dataPrefix(40)`→`.dataPrefix(cards.count)`. Each cell is a focusable UIHostingController, so realizing fewer per row is the dominant scroll-cost lever; a shelf is a preview, "Show all" covers the rest. Declined (recorded so not re-attempted blindly): dropping `.posterShadow()` on browse (it's brand — README.md:189-193 — and browse-only drop is the drift the redesign fixes); removing `.hoverEffect(.highlight)` (focus-appearance risk, held for on-device measurement); grow-on-scroll sentinel (mockup treats browse as finite/fully-present; `LazyVStack` already virtualizes rows; would add off-brand pop-in). Branch `claude/strange-cray-165196`; not yet built (no TVVLCKit in worktree — owner builds `main` on device).
- [x] Dev auto-login to home server on fresh install (`b6fdacdb`, `BrunoDevAutoLogin.swift`)
- [x] Custom shelf: trailing Show-all card, ~36 items, hide loose movies (`d0d6e32f`)
- [x] Collections page: category row + per-category capped shelves + Show-all → grid (roadmap §3)
- [x] Genres page: core-category panel + keyword-mapped sub-genre shelves (`BrunoGenresView.swift`, roadmap §4)
- [x] Decades "Show all" → shelf-per-decade via `BrunoBoxSetShelvesView` (roadmap §4)
- [x] Hero auto-cycle (8s `Timer.publish`, pause-on-focus, cross-fade backdrop) (`BrunoHeroView.swift`, roadmap §1)
- [x] Real device install succeeded; session persists across relaunch on ad-hoc-signed build (`docs/STATUS.md`)
- [x] Headless real-library E2E + mock snapshot harnesses (`bruno-verify/`, `BrunoAutoSignIn.swift`)
- [x] App-wide rebrand: accent `#A1CCE0`, Oswald/Inter fonts, BRUNO wordmark
- [x] Toolchain restored to as-authored on Xcode 26.3 (compat shims reverted)

## Backlog / deferred (from PRODUCT_SPEC §8 + BRUNO_NOTES §Deferred)

Deferred from the perf + browse-completeness pass (need owner decision or on-device measurement — NOT done):

- [ ] **Per-generator query-limit cut** (`BrunoQuery` 60→20 for spine `.query` shelves) — changes the *visible* per-shelf set, so it's a **content change** needing owner sign-off, not a silent perf tweak.
- [ ] **Reuse `BrunoPosterPrefetcher` on browse rows** — additive, but the benefit needs **on-device** (real Apple TV, Release) Animation-Hitches measurement; also interacts with the artCarousel cards' own focus-fetch model.
- [ ] **Static-collection-grid completeness** — `BrunoLibrarySnapshot.fetchChildren` `limit=200` feeds Directors/Studios/`BrunoStudiosGridView`; sub-group list `limit=100`. **LATENT** (nothing dropped at the ~635-movie/19-series scale); open design decision: cap-at-shelf vs lazy-fetch-at-destination. Bundle `BrunoBoxSetYearRangesViewModel.range` fix here: add `sortBy=[.productionYear]` so min/max are correct past 200 films (tiny).
- [ ] **Off-MainActor per-shelf shuffle/dedupe** — optional micro-opt, deferred.
- [ ] **On-device perf validation of the whole pass** — owner's ~165-hitches / 45–61ms Home target on a real Apple TV (Release).
- [~] **DISCUSS: UIKit-drawn shelf cells — the snappiness ceiling (NOT needed for now — superseded by the hosting-controller-reuse fork, `bebdfe30`)** — **2026-06-27:** the structural floor this item targets (the per-cell `UIHostingController` mint, below) is now lifted by the `DiplomacyMusic/CollectionHStack@bruno-hosting-reuse` fork (commit `466aeb3f`) — the hosting controller is kept alive across reuse and its `rootView` is swapped on recycle, so recycling DOES now amortize the SwiftUI-graph build, exactly the property this rearchitecture was after. Combined with the focus-gated genre cell (`7985aaf0`), this is expected to capture the perceptible win without the multi-week UIKit-cell rewrite. **Hold this item closed unless an on-device re-record (still outstanding) shows residual hitch the fork + focus-gate didn't remove.** Original analysis (kept for reference) — verified via swift-xcode-expert (2026-06-25, on-device HUD data: 60fps settled, ~20fps/50–117ms scrolling, single 1285ms·76f / 423ms·24f freezes on surface-enter / decade-select). Root cause is the per-cell `UIHostingController` mint: `HostingCollectionViewCell.setup` builds a fresh `UIHostingController` on **every** `cellForItemAt` and discards it in `prepareForReuse`, so recycling never amortizes the SwiftUI-graph build — the freeze scales with how many cells are built in one synchronous update, NOT with the collection topology. This cost is **identical in stock Swiftfin** (stock tvOS `HomeView` is the same `ScrollView{VStack{ForEach{PosterHStack}}}`; "see all" = one flat paged `CollectionVGrid`). The LePips package has **no** vertical-of-horizontal recycling primitive and `CollectionVList` is iOS-only, so the mooted "single recycling collection" rewrite (PERF_SHELVES #5) neither exists off-the-shelf nor removes the per-cell cost — **strike #5 / correct the PERF_SHELVES claim that stock uses one collection**. What Netflix/Apple-TV-app do that Bruno can't today: (a) truly-recycled lightweight UIKit cells (UIImageView+label, image swapped on reuse — no per-cell controller mint), (b) one `UICollectionViewCompositionalLayout` with `.orthogonalScrollingBehavior(.continuous)` per row = one reuse pool for the whole page, (c) zero layout/decode/effect work during the focus move (art pre-decoded off-main to exact size, prefetched ≥1 screen ahead, baked shadows). Matching that = drop the poster cell to a recycled UIKit cell under a compositional layout: **multi-week, highest regression risk** (focus engine, Show-all trailing card, gradient category row, zoom transitions, every INV-1..9). Cap-and-grow + cheaper-cell + browse-prefetch (below) capture MOST of the perceptible win without it — do those first, measure on-device, THEN decide whether the last increment of "butter" is worth the rewrite.
- [ ] **Hero left/right spotlight stepping via a UIKit responder** — `.onMoveCommand` was removed from `BrunoHeroView` (it's a CONSUMING responder that swallowed Up and trapped focus off the tab bar). To restore manual left/right stepping of the Home/Kids multi-item spotlight WITHOUT re-breaking Up: wrap the hero (or add an invisible sibling) in a `UIViewRepresentable` whose `UIView`/`UIControl` overrides `pressesBegan(_:with:)` — handle `.leftArrow`/`.rightArrow` (step the carousel) and call `super.pressesBegan` for `.upArrow`/`.downArrow`/`.select` so vertical moves + Select fall through to the focus engine (Up → tab bar). Verify on a real Apple TV; sim focus is unrepresentative.
- [ ] Direct hero-play (build `MediaPlayerItemProvider` → `.videoPlayer`; today routes to stock detail → Play)
- [ ] Localize Bruno UI strings via `L10n` (prototype is English-only; `hard_coded_display_string` disabled in Bruno views)
- [ ] Licensed Knockout font (Oswald is the brand stand-in)
- [ ] Seasonal BoxSet auto-surfacing (date-aware; Halloween in October, etc.)
- [ ] Formal XCTest target (today: `bruno-verify/` RNG checks + DEBUG self-check only)
- [ ] Open design questions from PRODUCT_SPEC §7 (Continue vs Up-Next merge, watched-dimming, studio card treatment, …)

---

## Enrichment → Bruno filing map (movie_features.json + GENRE_MAP.md)

> Authored 2026-06-26 by bruno-expert. Grounds the `MovieCollection/enrich/` enrichment (sub-genres,
> Oscar/Ebert, Foreign/per-language, Vibes, discovery genres, Movie Stars, Best-of-Decade, bruno-sig)
> against the **actual** Bruno code. Source of truth for HOW the enrichment files into the app.
>
> **The one architectural fact that governs everything:** Bruno's app-side shelf engine has **no tag
> source**. `BrunoQuery` (`Shared/Objects/Bruno/BrunoQuery.swift:20-56`) maps only `genres`,
> `studioIDs`, `personIDs`, `years`, `parentID`, `minCommunityRating`, `IsUnplayed`/`IsFavorite` →
> `BrunoQueryLibrary` (`BrunoQueryLibrary.swift:46-84`). `BrunoLibrarySnapshot` carries only group
> BoxSets + genre *names* + years (`BrunoLibrarySnapshot.swift:24-37`). **There is no `tags` field on
> the query, no `Tags`/`bruno-sig` in the snapshot, and nothing in Swift reads item tags today**
> (grep: zero `bruno-sig` consumers). ⇒ Anything that is "a membership of films selected by a tag" must
> become a **server-side Jellyfin BoxSet** to render with the engine as-is. App-side dynamic rows are
> only possible from sources the engine already speaks: genre-name, parentID (a BoxSet), year,
> rating, person, studio. **This is the spine vs. tail decision for the whole enrichment.**

### Decision table — where each enrichment category files in

| Enrichment category | Jellyfin BoxSet vs app-side | Surface(s) in Bruno | Code / script touch-point |
|---|---|---|---|
| **Sub-genre shelves** (superhero, noir, gangster, spy, alien, isolation, paranoia, war sub-tree…) | **BoxSet** (build script writes them as sub-collections under the **Genres** group, exactly like today's romance sub-genres) | Genre main screen (auto, as new shelf rows) + Home explore tail (auto, via the `genre`/`curated` generators that walk group children) | Script: `Build-Jellyfin-Collections.command` (add to the `add("genre",…)` pattern, lines ~258-300). **App: zero changes** — `BrunoBoxSetShelvesView` + `BrunoGenresView` already render every Genres-group child as a shelf. |
| **Two-tier model** (CORE = pills; sub/vibes/discovery = weighted rows) | both — CORE pills are app-side, weighted rows are BoxSets | Genre screen: CORE = `BrunoCoreGenre.all` pills; sub-genres = the shelf stack beneath, **weighted** by `childCount` | CORE pills: `BrunoGenresView.swift:33-45` (`BrunoCoreGenre.all`). Weighting: `BrunoCategoryShelves.weightedPreview` (`BrunoCategoryShelves.swift:402-413`) already ranks shelves/cells by `childCount^0.6`. See Q1 below. |
| **Oscar category collections** (Best Picture/Directing/Acting/Cinematography/Score/Screenplay) | **BoxSet** under **Curated** (or a new "Oscars" group) | Curated drill-in shelves + Home `curated` generator | Script only (Phase 4 `oscars.json` join). App: zero. |
| **Ebert Thumbs Up/Down** | **BoxSet** under **Curated** | Curated + Home tail | Script only (Phase 4 `ebert.json`). App: zero. |
| **Foreign Film + per-language** (Korean/Japanese/French…) | **BoxSet** (Foreign under Curated *or* a new top-level group; per-language as sub-collections) | Curated/Genre drill-in + Home. If promoted to its own top-level group tile → also a Collections-hub tile, auto-ranked. | Script writes collections. App: **only** if a NEW top-level group → add it to `BrunoCollectionCategory.rank` (`BrunoCategoryShelves.swift:91-101`) + an artwork case. Under Curated → zero app changes. |
| **Bruno Vibes** (indie_stress, hangout, mind_blower, ratatat) | **BoxSet** — one "Bruno Vibes" sub-group under **Curated** holding 4 collections | Curated drill-in + Home tail | Script only. App: zero. |
| **Personal genres** (cubicle, journalism, twee, oscar_bait, monster, snl, bromance…) | **BoxSet** under **Genres** (weighted rows, NOT pills — owner's two-tier rule) | Genre screen shelves + Home tail | Script only. App: zero. |
| **~65 discovery genres** (biopic, coming_of_age, sports…) | **BoxSet** under **Genres**, only the graduated ones | Genre screen shelves (weighted, sink low) + Home tail | Script only. App: zero. Owner picks which graduate (GENRE_MAP §Open decisions). |
| **Movie Stars master tile** (24 actors) | **BoxSet group** — a 7th/8th favorited group "Movie Stars" whose children are per-actor sub-collections, **structurally identical to Directors** | Collections hub tile + a "Browse by Stars" portrait shelf on Home (clone of "Browse by Director") | Script: clone the `director` CATS block keyed on `Type=="Actor"` cast (lines 163-183, 265-266). App: small — clone the auteurs shelf + a `movieStarBoxSets` snapshot accessor. See Q2 below. |
| **Best of [decade]** top row | app-side ranked row — but needs a **rank source** | First shelf inside a Decade drill-in + a promoted Home shelf | Needs bruno-sig (see Q3). Touch-point: `BrunoBoxSetShelvesView.loadYearShelves` / the decade overview (`BrunoBoxSetShelvesView.swift:402-440`). |
| **"[Year] in focus" Home shelf** | app-side — **already exists** | Home spine | `BrunoHomePlan.yearShelf` (`BrunoHomePlan.swift:371-383`); 3 promoted years in the spine (`build`, lines 59-172). Ranking by bruno-sig is the only enhancement. |
| **bruno-sig (significance)** | **server-side item field** (Tag or a custom field), consumed app-side | drives Best-of-Decade ordering + a "Cultural Touchstones" shelf | **Requires NEW plumbing** — the engine has no tag/sig source today. See Q3. The lowest-friction path is a **server BoxSet** ("Cultural Touchstones" / "Best of the 2000s") rather than an item-tag the app must learn to read. |
| **Cultural Touchstones** shelf | **BoxSet** under Curated (membership = top-bruno-sig films) | Curated + Home tail | Script computes membership from bruno-sig; writes a collection. App: zero. |

### Build-script-side (Jellyfin collections) vs app-side (Swift)

- **Build-script-side (the bulk — ~90% of this enrichment):** every membership-defined set becomes a
  Jellyfin BoxSet under one of the group tiles. The script already does exactly this pattern
  (`add(cat, clean, …)` → `/Collections?name=…&ids=…` → nest under the group tile via `add_items`,
  `Build-Jellyfin-Collections.command:170-342`). Sub-genres, Oscar, Ebert, Foreign, Vibes, discovery,
  personal genres, Cultural Touchstones, and the per-actor Movie Stars collections are **all script
  work**. **The app renders them with ZERO changes** because `BrunoCategoryShelves` /
  `BrunoBoxSetShelvesView` / `BrunoGenresView` walk group children generically and the Home explore
  generators (`genre`, `curated`, `studio`, `decade`, `seasonal`) seed off the snapshot's group lists.
- **App-side (small, surgical):** (1) Movie Stars group tile + "Browse by Stars" shelf (clone of
  Directors/auteurs). (2) Best-of-Decade row + bruno-sig consumption *if* sig ships as an item field
  rather than a BoxSet. (3) Adding a NEW top-level group (Foreign, Oscars) to `rank` + artwork *if*
  promoted out of Curated. (4) Optionally widening `BrunoCoreGenre.all` keyword buckets so new
  sub-genres land in the right CORE pill. Nothing here touches a perf invariant if done right (below).

### Tracker rows (this enrichment)

| St | Item | Where | Notes |
|----|------|-------|-------|
| [ ] | **Sub-genre / Oscar / Ebert / Foreign / Vibes / discovery collections** | `Build-Jellyfin-Collections.command` (owner-run) | Pure script work; app renders them free. Gated on GENRE_MAP §Open-decisions + Phase 4/5 datasets. |
| [ ] | **Movie Stars master tile** (clone of Directors) | Script: per-actor collections under a new "Movie Stars" group · App: `movieStarBoxSets` snapshot accessor + "Browse by Stars" shelf in `BrunoHomePlan` + Collections rank/artwork | Cleanly cloneable — see Q2. Both halves needed; ship together. |
| [ ] | **Best-of-[decade] top row + "[year] in focus" ranking** | App: `BrunoBoxSetShelvesView` (decade) + `BrunoHomePlan.yearShelf` · plumbing for bruno-sig | Blocked on the bruno-sig delivery decision (Q3). Year-in-focus row already exists; only ranking is new. |
| [ ] | **bruno-sig delivery decision** | owner + script vs app | **DECISION NEEDED:** ship sig as a server BoxSet ("Best of the 2000s", zero app work) OR as an item field the app must learn to read (new `BrunoQuery`/snapshot plumbing). Recommend BoxSet first. |
| [ ] | **New top-level group(s)?** (Foreign / Oscars / Movie Stars promoted) | `BrunoCollectionCategory.rank` + `BrunoCollectionArtwork` | Only if promoted out of Curated; else zero app work. |

### Perf-invariant flags (`docs/BRUNO_PERF_INVARIANTS.md`)

- **No conflict for the BoxSet-only work.** Adding more group children = more shelves; the shelf
  scaffold is already height-pinned (INV-1), id-stable (INV-2 — ids derive from `boxSet.id`), and
  cap-and-grown (`BrunoCategoryShelves.visibleShelfCount`). Genre-screen and Home shelf counts grow,
  but each shelf is a fixed-height, lazily-mounted row — no invariant breaks.
- **Watch the Home explore tail (INV-3).** The tail generators pick a *random* group child per slot
  (`BrunoHomePlan.boxSetShelf`, lines 396-414). Dozens of new sub-genre collections = a much larger
  random pool, which is fine and stays deterministic. **Do NOT** introduce nondeterministic selection
  (e.g. "show the highest-sig collection") into `build`/`appendExplore` without seeding it — that would
  break "same seed ⇒ same Home" and trip `BrunoHomePlan.selfCheckPassed()`.
- **Best-of-Decade ordering must be a pure function of (sig, snapshot)** to keep INV-3. If sig arrives
  as item data, sort by it deterministically (sig, then a stable tiebreaker) — never by a live/random
  server sort.
- **Movie Stars shelf is INV-safe** — it's the auteurs shelf shape (portrait `.items`, fixed height,
  stable id), already proven by Directors.
- **A bigger genre/curated child set does not change prefetch width (INV-4)** or the seed-keyed cache
  (INV-5) — those are per-cell/per-shelf-id and indifferent to how many shelves exist.

### Answers to the four questions (one-liners; full reasoning in the agent reply)

1. **Weighted/ranked rows on Home + Genre screen?** Yes — but the SOURCE is **collections (BoxSets),
   not tags**. `weightedPreview` (`BrunoCategoryShelves.swift:402-413`) already weights by `childCount`.
   Tag-driven rows are **not** possible without new plumbing; file tag-memberships as BoxSets.
2. **Directors tile cloneable for Movie Stars?** Yes, cleanly. It's a favorited group of sub-BoxSets
   (script) + a `directorBoxSets` snapshot accessor + an `appendItemsShelf` portrait row + a Collections
   rank/lens entry. Clone each. Lowest-risk app change in this whole plan.
3. **Best-of-Decade + year-in-focus wiring?** Year-in-focus already ships (`yearShelf`). Best-of-Decade
   = prepend a ranked row to the decade drill-in. Both need a rank signal. **Reconcile with bruno-sig by
   shipping sig as a server BoxSet first** ("Best of the 2000s") so the app renders it with zero new
   plumbing; defer the item-field path.
4. **Script-side vs app-side?** ~90% script (every membership set → BoxSet under a group tile, rendered
   free). App-side is only: Movie Stars tile+shelf, Best-of-Decade ordering, and any newly-promoted
   top-level group's rank/artwork.

---

## Spine vs tail (the product contract, condensed)

Home is a **stable spine** (shelves 2–10/11: Continue Watching → Up Next → New Releases → Director →
Genre → Series → Studio → Eras → Browse-by-Director → Collections) plus a **dynamic tail** of 5–6 seeded
"explore" shelves (+2 per scroll page, cap ~18). `seed = dayStamp()`, **Shuffle** re-rolls. `buildHome(seed)`
is pure given `(seed, librarySnapshot)`. Full taxonomy + generators: `PRODUCT_SPEC.md` §3–4.

## Guardrails (do not regress)

- **Additive, tvOS-only.** Bruno UI lives in `Swiftfin tvOS/Views/BrunoHomeView/` and `Shared/Objects/Bruno/`.
  iOS stays stock Swiftfin + rebrand. Don't touch player/nav/detail engine source.
- New files in the file-system-synchronized tvOS group → **no `.pbxproj` edits**. Non-Bruno edits stay
  DEBUG-gated and inert-by-default (see `SwiftfinApp.swift`).
- Never hardcode BoxSet/library IDs — discover groups dynamically (favorited BoxSets of BoxSets).
- **Before UX-polishing Home/browse shelves, read `docs/BRUNO_PERF_INVARIANTS.md`** (INV-1..9). The
  Home perf rests on non-obvious invariants (fixed row height, stable shelf ids, prefetch width ==
  cell width, seed-keyed/source-restricted cache); `// INV-n` anchors mark each site. Restyle freely —
  just keep those nine intact.
  - **Owner-override exception:** `BrunoStudiosGridView` *intentionally* breaks **INV-6** (scroll-coupled
    blur over a `LazyVGrid`) for the demanded "image descends into the grid" effect. **Do NOT** revert it
    to a sibling layer — the tuning lever is the hero header height/blur, not the structure.
- No secrets in the repo. Live creds live only in gitignored `bruno_jellyfin.env`.
- Land finished work on **`main`** — the owner builds `main` in Xcode.
